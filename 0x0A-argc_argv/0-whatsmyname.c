#include <stdio.h>
/**
 * main - prints the name of a program
 * @agrc: counts each agurment
 * @agrv: prints each aguement
 * Return: 0 success
 */
int main(int argc, char argv[])
{
	printf("%s\n", agrv[0]);
	return (0);
}
