#include "main.h"

/**
 * add - function that adds two integers and returns the result.
 * @b: second parameter
 * @a: first parameter
 * Return: answer.
 */

int add(int a, int b)
{
	int answer;

	answer = a + b;
	return (answer);
}
