#include "main.h"

/**
 * _abs - function that computes the absolute value of an integer.
 * @number: The input number to check
 * Return: answer
 */

int _abs(int number)
{
	int answer;

	if (number < 0)
	{
		answer = number * (-1);
		return (answer);
	}
	else
		return (answer);
}
