#include "main.h"

/**
 * _islower - checks for lowercase character
 *@c: the character to be check
 * Return: 1 if c is lowercase, else return c
 */
int _islower(int c)
{
	return (c >= 'a' && c <= 'z');

}
