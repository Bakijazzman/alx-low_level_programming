#include "main.h"
#include <stdio.h>
/**
 * mul - function that multiplies two integers
 * @a: first  parameter
 * @b: second parameter
 * Return: result
 */
int mul(int a, int b)
{
	int result = a * b;

	return (result);
}
